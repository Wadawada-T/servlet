实现步骤：
   1.数据库、表、记录、 创建项目  jar文件、工具类、配置文件
   2.登录页面，与表对于的实体类，servlet service dao
   3.检查页面中的表单信息是否 完整，（表单元素中的属性要完整，提交路径要设置好）
   4.检查web.xml中的配置请求的路径是否与表单中的提交路径一致
   5.servlet获取请求参数（用户名和密码）
   6.servlet层依赖service检查是否可以登陆成功
   7.service 依赖dao与数据库进行交互，获取返回结果集，处理业务逻辑
   8.dao层使用c3p0+DBUtils与数据库进行交互
   9.servlet获取到结果集，向浏览器做出响应
   
   
   
